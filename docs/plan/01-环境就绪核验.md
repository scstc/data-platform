# 环境就绪核验记录(前置工作)

> 核验日期:2026-06-08。目的:确认 06-09 D1 可直接开工,清掉引擎风险。结论:**全绿,可开工。**

## 工具链
| 工具 | 版本 | 备注 |
|------|------|------|
| docker | 29.4.3 | ✅ |
| uv | 0.11.7 | ✅ |
| node / npm | 26.0.0 / 11.12.1 | ✅ |
| 系统 python3 | 3.14.4 | 仅参考;各项目用各自 venv |

## 组件就绪状态
| 组件 | 状态 | 证据 |
|------|------|------|
| backend venv | ✅ Python 3.12.12 | `backend/.venv` |
| backend .env | ✅ 已存在 | `DATABASE_URL` 指向 :55433 |
| 初始迁移 | ✅ `0001_initial (head)` | `alembic current` |
| data-juicer venv | ✅ Python 3.11.14 | `data-juicer/.venv` |
| frontend 依赖 | ✅ node_modules 已装 | — |

## 核验通过项
1. **PG 容器**:`docker compose up -d`(backend/)→ `adp-postgres` 秒 healthy,:55433。
2. **数据库迁移**:`alembic upgrade head` → 4 张表 `alembic_version / datasources / ingest_tasks / upload_records`。
3. **后端服务**:uvicorn :18003 → `GET /healthz` = `{"status":"ok"}`;`GET /api/v1/datasources` = `{"data":[],"total":0,"success":true}`(契约正确)。
4. **⭐ data-juicer 引擎端到端**:`dj-process --config demos/process_simple/process.yaml` → 退出码 0,3.5s 完成,6 条 → 过滤出 2 条中文。

## ⭐ D1 关键发现(直接定调集成方案)
- **子进程调用可行且最简**:`dj-process --config <yaml>` 一行跑通,退出码 0 即成功 → **D1 引擎调用边界建议直接走子进程**(无需先纠结 /dj-api)。
- **产物结构**(每次 run 落在时间戳目录,含 `logs/`):
  - `demo-processed.jsonl` —— 清洗后数据(#7 清洗产物)
  - `demo-processed_stats.jsonl` —— **逐条 stats**,如 `{"__dj__stats__":{"lang":"zh","lang_score":0.945}}` → **#6 质量逐条打分白捡**
- **离线可跑**:fasttext 语言模型已缓存,无需联网下载。

## 明天 D1 开工前置(已就绪,记录启动命令)
```bash
# 1. 起 PG(已在运行,重启电脑后需重跑)
cd backend && docker compose up -d
# 2. 迁移(已是 head,新增迁移时跑)
.venv/bin/alembic upgrade head
# 3. 后端
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 18003   # /docs 看 Swagger
# 4. 前端(真实后端)
cd ../frontend && npm run dev        # :8001,MOCK=none
# 5. data-juicer 引擎冒烟
cd ../data-juicer && .venv/bin/dj-process --config demos/process_simple/process.yaml
```

## 残留/注意
- adp-postgres 当前**仍在运行**(供明天直接用)。
- 系统 python 是 3.14,务必用各项目 venv,勿用系统解释器。
- data-juicer demo 输出堆在 `data-juicer/outputs/`(已有多次 run),正式接入时平台产物应落到独立目录并入库,勿与 demo 混。
